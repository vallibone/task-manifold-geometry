# Experiment 1: Task Structure

Compares intrinsic arc-aligned labels against extrinsic coordinate-based labels across circle, spiral, and sinewave manifolds.

Run:

```bash
python experiments/exp1_task/run.py
```
