# Experiment 2: Capacity Allocation

Tests whether redistributing hidden-layer capacity changes learned geometry when the total parameter budget is fixed.

Run:

```bash
python experiments/exp2_capacity/run.py
```
